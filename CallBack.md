## Callback in JavaScript

🔹 Callback কী? 
👉 Callback হলো এমন একটি ফাংশন, যেটাকে অন্য একটি ফাংশনের আর্গুমেন্ট হিসেবে পাঠানো হয়,
এবং সেই ফাংশনের ভেতরে পরে execute (চালানো) করা হয়।

